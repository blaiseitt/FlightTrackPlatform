server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native #local configuration
  cloud:
    config:
        server:
          native:
            search-locations: file:../config

management:
  endpoints:
    web:
      exposure:
        include: health,info